# Liquidsoap with funk.liq baked in (Coolify can't bind-mount the single repo
# file at runtime). Mirrors the raw-compose volume mount of funk.liq.
FROM savonet/liquidsoap:v2.2.5
COPY funk.liq /etc/liquidsoap/funk.liq
